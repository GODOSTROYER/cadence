# Dependency requests

Builders append one line per requested dependency (package, version constraint, why, which module).
The orchestrator reviews and adds approved ones to `pyproject.toml` / `ui/package.json`.

- (none yet)

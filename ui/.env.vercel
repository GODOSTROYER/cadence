# Loaded by `vite build --mode vercel`: static evaluation data + live agent behind the function, served under
# /hiver-assignment/ so www.arnavbule.in can proxy it at that path.
VITE_STATIC=1
VITE_LIVE_HANDLE=1
VITE_BASE=/hiver-assignment/
